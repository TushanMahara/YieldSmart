# YieldSmart Environment Variables Example
# Copy this file to .env and configure as needed:

# Optional GitHub Personal Access Token (for remote dataset access if required)
GITHUB_TOKEN=your_token_here

# Flask Server Port (defaults to 5000)
PORT=5000
